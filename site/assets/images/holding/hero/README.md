# Holding page hero image

Place the holding-page hero background image here (e.g. `hero.jpg`).

Path is configured in `data/holding/variables.py` as `hero_image_path = "holding/hero/hero.jpg"`. Rename or update the variable if you use a different filename.
